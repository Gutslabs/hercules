.DS_Store

# Xcode
build/
dist/
DerivedData/
*.xcuserstate
xcuserdata/
*.moved-aside
*.xccheckout
*.xcscmblueprint
*.xcarchive
*.dSYM/
*.app
*.ipa
*.dmg

# Swift Package Manager
.build/
.swiftpm/

# macOS
*.swp
*.lock

# Local agent/editor state
.claude/
.codex/

# Secrets, signing material, and local config
.env
.env.*
*.pem
*.p8
*.p12
*.cer
*.key
*.mobileprovision
*.provisionprofile
codex_auth.json

# Local app data / exports
*.store
*.store-shm
*.store-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal
hercules_backup*.json
HerculesBackup*.json

# On-device TTS model (sherpa-onnx Piper dfki) — büyük (79MB) + CC-BY-NC-SA (ticari-yasak), repoya konmuyor.
# Taze clone sonrası: scripts/fetch-tts-model.sh ile indir.
Hercules/Resources/tts/

# sherpa-onnx vendored xcframework'ler (~347MB ikili) — repoya konmuyor.
# Taze clone sonrası: scripts/setup-sherpa.sh ile indir. (vendor/SherpaOnnx/ paketi tracked kalır.)
vendor/sherpa/

# Geçici proje-düzenleme script'leri (xcodeproj mutasyonu)
scripts/_*.rb

# Kod kapsama artefaktı (xcodebuild bırakıyor)
*.profraw
