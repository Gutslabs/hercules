//  BottomCenterChatDock.swift
//
//  Alt-orta AI dock'u kaldırıldı (2026-06-10) — sohbet artık yalnız iki yüzeyde:
//  sağ panel (ChatSidebar) + tam sayfa (ChatPageView). Bu dosya boş bırakıldı;
//  .xcodeproj'a iki target üzerinden bağlı olduğu için diskten silinmedi
//  (file ref bozulmasın diye). İçerik kasıtlı olarak boş.

import SwiftUI
